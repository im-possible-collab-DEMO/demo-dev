spring.application.name=backend

# --- Database (MySQL) ---
# 로컬에 demodev 데이터베이스를 미리 만들어두세요: CREATE DATABASE demodev;
spring.datasource.url=jdbc:mysql://localhost:3306/demodev?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=2535
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- JWT ---
# 운영 배포 전에는 환경변수(JWT_SECRET)로 교체하세요.
jwt.secret=R7BZkRUcrZSMrRjwwAXl+0XUxD4/ikL/jTs9nRQ0Z7s=
jwt.expiration-ms=86400000

# --- CORS ---
cors.allowed-origins=http://localhost:5173
